{% extends "base.html.twig" %}
{% block title %}<title>Panneau de bord</title>{% endblock %}
{% block page_content %}
    <main class="d-flex align-items-center">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-12 col-md-10 col-lg-8">
                    <div class="row g-3">
                        <div class="col-12 col-sm-6">
                            <a class="btn col btn-info btn-lg w-100 py-4" href="{{ path('listeUtilisateurs') }}">Liste
                                des utilisateurs</a>
                        </div>
                        <div class="col-12 col-sm-6">
                            <a class="btn col btn-info btn-lg w-100 py-4" href="{{ path('classes') }}">Groupes de classe</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
{% endblock %}